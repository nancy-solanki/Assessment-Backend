from .base import *

# Local development settings
DEBUG = True

ALLOWED_HOSTS = ['*']

# CORS settings for development (allow all for easier local testing)
CORS_ALLOW_ALL_ORIGINS = True
CORS_ALLOW_CREDENTIALS = True
